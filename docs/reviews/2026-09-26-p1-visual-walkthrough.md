# P-1 视觉走查轮 — 9 view 真机目检 + Agent/声文专属走查

**日期**:2026-09-26　**条款**:PRD R183(R177 方案 P-1 批次)
**链路**:`yarn build` 后经 CDP(9281) 驱动 `out/main/index.js` 截图;9 view @1440×900 + Agent/声文 8 态(空态/长文本/错误态/窄窗 980×860/静息/草稿);**独立 `--user-data-dir`** 隔离真机用户数据,长文本/错误态经手工伪造 `agent-sessions/*.jsonl` 会话恢复驱动(事件形状对齐 `AgentEvent` 联合类型)。
**评审方式**:双视觉通道(zai/4.5v)交叉评审,共 17 份报告;9/9 view + 8/8 态全覆盖。

---

## §1 缺陷清单与处置

### 1A. Agent 工作台(本批修复 ✅ → P-2 继续承接工具卡专项)

| # | 缺陷 | 根因 | 处置 |
|---|---|---|---|
| A1 | **头重脚轻**:空/短会话时输入行悬在视口 2/3 处,底部 ~300px 空置;空态不垂直居中 | `.agent-log` `max-height:480px` 钳制 + keep-alive 包裹 div 无 flex 撑满 → `.agent-tab` 只随内容长高 | **修**:keep-alive 包裹加 `.ai-keep-pane`(flex:1 列容器);`.agent-tab` flex:1;`.agent-log` 去 cap 改 `min-height:0` 内滚 → 输入沉底、空态居中、长会话在视口内滚 |
| A2 | 会话列表无选中态(恢复后看不出当前会话) | `loadSession` 不落 state,按钮无 active 样式 | **修**:`activeSessionId` state + `.active` 高亮 |
| A3 | **错误终态不可见**:错误会话恢复后零错误标识;live 错误也只有一行灰 hint | `loadSession` 不恢复 `done{reason:error}`;错误显示用 `.ai-hint-line` 弱样式 | **修**:恢复会话时回放末次 done 错误;错误升级 `.agent-error-banner`(红 tint 底+边框+⚠ 前缀文案) |
| A4 | textarea 保留原生 resize 手柄;发送按钮贴底不居中 | CSS 默认 | **修**:`resize:none` + 按钮 `margin-bottom` 光学对齐 |
| A5 | 空态三步引导是全页最暗小字(WCAG 贴线),恰是关键引导 | 颜色/字号偏弱 | **修**:对比度提至 0.75 白、字号 12→12.5 |
| A6 | 工具输出滚动条为亮色线,深色主题突兀 | pre 未定义 scrollbar | **修**:thin 深色滚动条(agent-log/pre/sessions 统一) |

> 工具卡无徽标/长输出主导 40%/无折叠提示/diff 仅 before/after 双 pre——**P-2 批次专项**(R184),本批不动避免双改。

### 1B. 声文(本批修复 ✅)

| # | 缺陷 | 根因 | 处置 |
|---|---|---|---|
| V1 | **逐文件行右列系统性截断**("291.0 MB / 29[1]"、"0.0 MB / [0]",1440/980 两宽度均在) | 行内 nowrap 尾簇总宽 > 行宽,溢出被容器裁切 | **修**:行改 `[glpyh][path flex-shrink+ellipsis][meta 尾簇 nowrap]`,内联条 64→44px |
| V2 | config.json 显示 "0.0 MB / 0"(清单 5KB) | 小文件 MB 格式化丢精度 | **修**:`fmtBytes`(<1MB 显示 KB),完成态单值、未完成 `cur / total` |
| V3 | 「语速」标签被压成竖排两行;`1.00×` 粘连滑块 | label 无 nowrap、无间距 | **修**:nowrap + 间距 |
| V4 | 分句列表无可供性:纯文本行、无序号、无省略号(120 字硬切)、无卡片边界 | CSS 过素 | **修**:序号+行卡片+省略号+title+engine 限定 cursor |
| V5 | 词典空态/占位符/「中文·N 句」对比度低 | 颜色偏暗 | **修**:统一提一档 |
| V6 | 0% 时总进度条与分隔线无法区分 | 轨道与背景同明度 | **修**:轨道加描边/提亮一档 |

> 系统引擎播放键在 0 句时**已**禁用(评审误读,禁用样式偏弱属 V5 范畴一并提对比);语言标签对混合文本标 EN 为 heuristic by-design,入档。

### 1C. 全局 chrome(本批修复 ✅,基线随之更新)

| # | 缺陷 | 出现视图 | 处置 |
|---|---|---|---|
| C1 | 顶栏标题"假居中"——flex:1 居中受左右两侧不等宽影响,中心在 660~700px 漂移,无任何对齐参照 | dashboard/effects/video/audio/architecture/diagnostics/ai(7/9) | **修**:`position:absolute; left:50%` 真窗口居中 |
| C2 | rail 标签 **9px**(0.5625rem)小字,多视图报"接近不可读" | 全部 | **修**:9→11px(72px 宽 rail 5 字标签实测容纳) |
| C3 | 效果库底部一排卡片硬切,无渐隐提示 | effects | **修**:滚动容器底部 28px 渐隐 mask |
| C4 | 音频可视化大区域(占屏 ~55%)空态零信息 | audio | **修**:未播放时叠加空态引导文案 |

### 1D. 入档(不修,附理由与去向)

| # | 事项 | 理由/去向 |
|---|---|---|
| D1 | 模块区 8 卡 8 色相"彩虹化";≥3 种绿并存;字号阶梯 12/13/14/16/18 过密;状态卡/模块卡圆角不一 | **设计系统级**,需产品拍板统一 token;归入后续设计系统专项(建议 P-7 扩展) |
| D2 | 顶栏标题与页面 H1 冗余(9 视图双标题) | 信息架构决策(去顶栏 or 弱化),牵动全局导航语义,不属打磨批次 |
| D3 | 手势助手悬浮胶囊贴边/遮挡内容(effects/games) | 悬浮交互形态决策;如改需全局安全区方案 |
| D4 | workspace 左面板「调节」底部截断无滚动提示;diagnostics「环境」卡片底部裁切 | 均为滚动容器边缘提示缺失,应随 C3 的 mask 模式做**全视图滚动提示统一轮**,单独逐个改易漏 |
| D5 | games 卡片描述行数不齐/底部徽章参差;下半屏空旷;architecture 标签脱节/构图偏左上 | 内容驱动布局(描述字数不一)+3D 场景编排,归 games/architecture 各自专项 |
| D6 | ai-config 表单右半 ~300px 空白 | 表单布局重构(两列/卡片化)属设计系统 |
| D7 | 声文混合语言文本标「EN」 | detectLang heuristic(CJK×1.5 权重)by-design |
| D8 | 顶栏「关」胶囊语义不明 | 全局开关命名/形态决策,牵动 onboarding 文案 |
| D9 | dashboard 状态卡行与模块网格右缘差 ~220px | dashboard 版式重构(5 卡铺满 or 4+1),需版式决策;**与 D1 同批** |
| D10 | workspace toast 常驻叠「手动助手」按钮 | toast 生命周期属功能逻辑(自动消失),非纯样式 |

---

## §2 验证结果(已执行)

1. **typecheck 双绿**;`yarn test` **131 文件 / 1170 用例全过**(+2 新增:formatBytes 单测、会话恢复选中态+错误横幅回归);
2. **客观 DOM 断言 10/10 PASS**(CDP 驱动复测):顶栏标题中心 720.0px(窗口正中)、rail 标签 11px、输入行距视口底 20px(沉底)、agent-log 高 636px(撑满)、会话 active 类在场、错误横幅恢复在场、模型行 0/7 溢出(尺寸样本 "0 KB / 5 KB")、语速标签单行 16px、分句序号在场、音频空态提示在场;
3. `ui:snapshot` 首跑 GATE FAIL(全视图 0.37–0.52% 漂移 = C1/C2 全局 chrome + effects mask/audio 空态/ai tab 预期变更),核对后 `--update-baseline` 重立基线于 HEAD a6bbc1d,复跑 **9/9 GATE PASS 0.0000%**;
4. 复核走查截图(9 view + Agent/声文 8 态)重截存档于会话 temp;视觉复核通道(两个 MCP)当日对新增图持续 400,以 DOM 断言 + 既有 17 份评审报告交叉销项代替。

## §3 结论

骨架层(网格纪律、语义色大方向、无文字截断事故)扎实;Agent/声文两 Tab 存在 1 个布局级缺陷(A1)与 1 个信息级缺陷(A3)+ 一批对比度/可供性小缺陷,全部本批归零;全局 chrome 修 4 项(C1-C4),其余 10 项入档并给出去向。工具卡体验缺口(折叠/徽标/diff)按计划归 P-2。
