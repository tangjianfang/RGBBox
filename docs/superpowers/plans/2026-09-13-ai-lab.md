# R88 AI 实验室实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 AI 实验室独立模块（rail 入口 + 四区视图 + 2 条新 IPC + Key safeStorage 加密落盘 + 厂商预设），设置页 AI 组迁走。

**Architecture:** shared 层加类型/校验/预设纯数据 → main 层抽 `chatCompletion` 通用管线并接 safeStorage 编解码 → preload 白名单 +2 → 渲染层 `AiLabView` 自管配置生命周期，App.tsx 删旧 aiCfg 状态。

**Tech Stack:** Electron（safeStorage）+ React 18 + TS；vitest（node/happy-dom）；lucide `Bot`。

**Spec:** `docs/superpowers/specs/2026-09-13-ai-lab-design.md`

## Global Constraints

- 提交标题 `[PRD-0002] <type>: <subject>`，可附 `(R88)`。
- `aiCleanupText`/`aiTranslateText` 对外签名与行为零改动（既有测试必须保持绿）。
- i18n zh+en 同步；品牌名/模型版本号不进 i18n。
- 不改 package.json scripts；测试命令 `yarn test <file>` / `yarn typecheck` / `yarn build`。
- 现状锚点：`aiCleanupService.ts` 123 行（buildCleanupRequest/buildTranslateRequest/parseCleanupResponse/postChat/cleanupOcrText/translateOcrText/DEFAULT_AI_SETTINGS.model='glm-4-flash'）；preload ai 方法在 180-187 行；main ai handler 在 index.ts 335-356；`asAiSettings` 在 index.ts 330；测试桩 `tests/renderer/setup.ts`（lucide NAMES 白名单）、`tests/renderer/_helpers.tsx`（rgbbox mock，ai 系在 82-86）。

## File Structure

```text
新增：src/shared/aiChatValidation.ts + tests/shared/aiChatValidation.test.ts
      src/shared/aiProviders.ts      + tests/shared/aiProviders.test.ts
      src/main/aiSecretCodec.ts      + tests/main/aiSecretCodec.test.ts
      src/renderer/src/components/AiLabView.tsx + tests/renderer/components/AiLabView.test.tsx
修改：src/shared/types.ts（+AiChatMessage/AiChatOutcome）
      src/shared/ipc.ts（+2 通道）
      src/main/aiCleanupService.ts（+chatCompletion/buildTestMessages/testConnection；cleanup/translate 复用；默认模型 glm-5.3-flash）
      src/main/index.ts（+2 handler；safeStorage 编解码接线）
      src/preload/index.ts（+2 白名单方法）
      src/renderer/src/hooks/tabNavigation.ts（MODULE_VIEWS += 'ai'）
      src/renderer/src/components/shellModules.ts（CARD_VIEWS/MODULE_META += ai）
      src/renderer/src/App.tsx（+view 渲染；删 aiCfg 系）
      src/renderer/src/components/SettingsView.tsx（删 AI 组）
      src/renderer/src/i18n/index.tsx（增删 key）
      tests/renderer/setup.ts（Bot 桩）、tests/renderer/_helpers.tsx（mock +2）
      tests/renderer/{i18nShellKeys.test.ts, components/shellModules.test.ts, components/SettingsView.test.tsx}（更新）
      docs/prd/PRD-0002-rgbbox-project-catalog.md（R88.9 收尾）
```

---

### Task 1: shared 纯数据层（类型 + 通道 + 校验 + 预设）

**Files:**
- Modify: `src/shared/types.ts`（文件末尾追加）、`src/shared/ipc.ts`（aiTranslateText 行后）
- Create: `src/shared/aiChatValidation.ts`、`src/shared/aiProviders.ts`
- Test: `tests/shared/aiChatValidation.test.ts`、`tests/shared/aiProviders.test.ts`

**Interfaces:**
- Produces: `AiChatMessage`/`AiChatOutcome`（shared/types）；`aiTestConnection`/`aiChat` 通道名；`validateChatMessages(v: unknown): AiChatMessage[] | null`；`AI_PROVIDER_PRESETS`/`AiProviderPreset`/`matchProviderPreset(baseUrl: string): AiProviderPreset`

- [ ] **Step 1: 写两个失败测试**

```ts
// tests/shared/aiChatValidation.test.ts
import { describe, it, expect } from 'vitest'
import { validateChatMessages } from '../../src/shared/aiChatValidation'

const ok = [{ role: 'user', content: 'hi' }, { role: 'assistant', content: 'hello' }]

describe('validateChatMessages (R88)', () => {
  it('accepts a valid conversation', () => {
    expect(validateChatMessages(ok)).toEqual(ok)
  })
  it('rejects non-array / bad role / bad content / system+user mix rules', () => {
    expect(validateChatMessages('nope')).toBeNull()
    expect(validateChatMessages([{ role: 'admin', content: 'x' }])).toBeNull()
    expect(validateChatMessages([{ role: 'user', content: 42 }])).toBeNull()
    expect(validateChatMessages([{ role: 'user' }])).toBeNull()
  })
  it('rejects >40 turns or >32k chars per turn', () => {
    expect(validateChatMessages(Array.from({ length: 41 }, () => ({ role: 'user', content: 'x' })))).toBeNull()
    expect(validateChatMessages([{ role: 'user', content: 'x'.repeat(32_001) }])).toBeNull()
    expect(validateChatMessages(Array.from({ length: 40 }, () => ({ role: 'user', content: 'x' })))).not.toBeNull()
  })
})
```

```ts
// tests/shared/aiProviders.test.ts
import { describe, it, expect } from 'vitest'
import { AI_PROVIDER_PRESETS, matchProviderPreset } from '../../src/shared/aiProviders'

describe('AI_PROVIDER_PRESETS (R88)', () => {
  it('has unique ids; every non-custom preset has baseUrl + models; custom exists', () => {
    const ids = AI_PROVIDER_PRESETS.map((p) => p.id)
    expect(new Set(ids).size).toBe(ids.length)
    expect(ids).toContain('custom')
    for (const p of AI_PROVIDER_PRESETS) {
      if (p.id === 'custom') continue
      expect(p.baseUrl).toMatch(/^https?:\/\//)
      expect(p.models.length).toBeGreaterThan(0)
    }
  })
  it('zhipu preset carries the models the user runs', () => {
    const zhipu = AI_PROVIDER_PRESETS.find((p) => p.id === 'zhipu')!
    expect(zhipu.models).toContain('glm-5.3')
    expect(zhipu.models).toContain('glm-5.3-flash')
  })
})

describe('matchProviderPreset', () => {
  it('matches by baseUrl and falls back to custom', () => {
    expect(matchProviderPreset('https://open.bigmodel.cn/api/paas/v4').id).toBe('zhipu')
    expect(matchProviderPreset('https://api.deepseek.com').id).toBe('deepseek')
    expect(matchProviderPreset('https://unknown.example.com/v1').id).toBe('custom')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `yarn test tests/shared/aiChatValidation.test.ts tests/shared/aiProviders.test.ts`
Expected: FAIL ×2（模块不存在）

- [ ] **Step 3: 实现**

`src/shared/types.ts` 末尾追加：

```ts
// ── R88: AI Lab chat IPC types ────────────────────────────────────────────
export interface AiChatMessage {
  role: 'user' | 'assistant' | 'system'
  content: string
}
export interface AiChatOutcome {
  ok: boolean
  /** Reply text when ok; empty string on failure. */
  text: string
  hint?: 'nokey' | 'auth' | 'http' | 'parse' | 'network'
  latencyMs: number
}
```

`src/shared/aiChatValidation.ts`：

```ts
// R88: preload-side guard for aiChat payloads — pure, shared with unit tests.
import type { AiChatMessage } from './types'

const MAX_TURNS = 40
const MAX_CHARS = 32_000
const ROLES = new Set(['user', 'assistant', 'system'])

/** Returns the validated conversation, or null when the payload is invalid. */
export function validateChatMessages(v: unknown): AiChatMessage[] | null {
  if (!Array.isArray(v) || v.length === 0 || v.length > MAX_TURNS) return null
  const out: AiChatMessage[] = []
  for (const item of v) {
    if (item === null || typeof item !== 'object') return null
    const { role, content } = item as { role?: unknown; content?: unknown }
    if (typeof role !== 'string' || !ROLES.has(role)) return null
    if (typeof content !== 'string' || content.length > MAX_CHARS) return null
    out.push({ role: role as AiChatMessage['role'], content })
  }
  return out
}
```

`src/shared/aiProviders.ts`：按 spec §3.5 全量实现（`AiProviderPreset{ id; label; baseUrl; models }` + 7 条预设 + `matchProviderPreset`：精确匹配 `baseUrl`，无匹配返回 custom 项）。

`src/shared/ipc.ts` 的 `aiTranslateText` 行后追加：

```ts
  aiTestConnection: 'rgbbox:ai:test-connection',
  aiChat: 'rgbbox:ai:chat',
```

- [ ] **Step 4: 验证 + 提交**

Run: `yarn test tests/shared/aiChatValidation.test.ts tests/shared/aiProviders.test.ts && yarn typecheck`
Expected: 全 PASS + 0 error

```bash
git add src/shared tests/shared
git commit -m "[PRD-0002] feat: R88 shared 层——chat 类型/校验/厂商预设/通道"
```

---

### Task 2: main 服务层（chatCompletion 抽取 + 加密 codec + 默认模型升级）

**Files:**
- Modify: `src/main/aiCleanupService.ts`
- Create: `src/main/aiSecretCodec.ts`
- Test: `tests/main/aiCleanupService.test.ts`（先查现状：若 R83/R84 已有此文件则在其中**追加**用例，不改既有用例）、`tests/main/aiSecretCodec.test.ts`

**Interfaces:**
- Consumes: `AiChatMessage`/`AiChatOutcome`（Task 1）
- Produces: `chatCompletion(messages: AiChatMessage[], s: AiCleanupSettings, opts?: { maxTokens?: number; temperature?: number; timeoutMs?: number }): Promise<AiChatOutcome>`；`buildTestMessages(): AiChatMessage[]`；`testConnection(s): Promise<AiChatOutcome>`；`DEFAULT_AI_SETTINGS.model = 'glm-5.3-flash'`；`encodeApiKey(plain: string, codec: SafeStorageCodec): string` / `decodeApiKey(stored: string, codec: SafeStorageCodec): string` / `interface SafeStorageCodec { encrypt(plain: string): Uint8Array | null; decrypt(data: Uint8Array): string | null }`
- 不变：`buildCleanupRequest`/`buildTranslateRequest`/`parseCleanupResponse`/`cleanupOcrText`/`translateOcrText` 签名与行为（内部改走 chatCompletion 后剥掉 latencyMs）

- [ ] **Step 1: 写失败测试（新增用例）**

`tests/main/aiCleanupService.test.ts` 追加（文件不存在则新建，头注 R88）：

```ts
import { describe, it, expect, vi, afterEach } from 'vitest'
import { chatCompletion, buildTestMessages, testConnection, DEFAULT_AI_SETTINGS } from '../../src/main/aiCleanupService'

afterEach(() => vi.unstubAllGlobals())

const settings = { baseUrl: 'https://x.example/v4', apiKey: 'k', model: 'glm-5.3-flash' }

describe('chatCompletion (R88)', () => {
  it('returns ok + text + latencyMs on 200 with choices', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => new Response(
      JSON.stringify({ choices: [{ message: { content: 'pong' } }] }), { status: 200 }
    )))
    const out = await chatCompletion([{ role: 'user', content: 'ping' }], settings)
    expect(out.ok).toBe(true)
    expect(out.text).toBe('pong')
    expect(out.latencyMs).toBeGreaterThanOrEqual(0)
  })
  it('classifies auth (401) and http (500)', async () => {
    vi.stubGlobal('fetch', vi.fn(async () => new Response('x', { status: 401 })))
    expect((await chatCompletion([{ role: 'user', content: 'p' }], settings)).hint).toBe('auth')
    vi.stubGlobal('fetch', vi.fn(async () => new Response('x', { status: 500 })))
    expect((await chatCompletion([{ role: 'user', content: 'p' }], settings)).hint).toBe('http')
  })
  it('nokey without api key; parse on bad json shape; network on fetch throw', async () => {
    expect((await chatCompletion([{ role: 'user', content: 'p' }], { ...settings, apiKey: '' })).hint).toBe('nokey')
    vi.stubGlobal('fetch', vi.fn(async () => new Response(JSON.stringify({ nope: 1 }), { status: 200 })))
    expect((await chatCompletion([{ role: 'user', content: 'p' }], settings)).hint).toBe('parse')
    vi.stubGlobal('fetch', vi.fn(async () => { throw new Error('offline') }))
    expect((await chatCompletion([{ role: 'user', content: 'p' }], settings)).hint).toBe('network')
  })
})

describe('testConnection (R88)', () => {
  it('pings with a tiny user message and max tokens', async () => {
    let captured: any
    vi.stubGlobal('fetch', vi.fn(async (_u: string, init: any) => {
      captured = JSON.parse(init.body)
      return new Response(JSON.stringify({ choices: [{ message: { content: 'pong' } }] }), { status: 200 })
    }))
    expect(buildTestMessages()).toEqual([{ role: 'user', content: 'ping' }])
    const out = await testConnection(settings)
    expect(out.ok).toBe(true)
    expect(captured.max_tokens).toBe(8)
  })
})

describe('DEFAULT_AI_SETTINGS (R88.5)', () => {
  it('default model is glm-5.3-flash on the zhipu endpoint', () => {
    expect(DEFAULT_AI_SETTINGS.model).toBe('glm-5.3-flash')
    expect(DEFAULT_AI_SETTINGS.baseUrl).toBe('https://open.bigmodel.cn/api/paas/v4')
  })
})
```

`tests/main/aiSecretCodec.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { encodeApiKey, decodeApiKey, ENC_PREFIX, type SafeStorageCodec } from '../../src/main/aiSecretCodec'

// deterministic fake codec: xor each char code
const fake: SafeStorageCodec = {
  encrypt: (s) => (s === '' ? null : Uint8Array.from(s, (c) => c.charCodeAt(0) ^ 0x5a)),
  decrypt: (b) => String.fromCharCode(...Array.from(b, (x) => x ^ 0x5a)),
}

describe('aiSecretCodec (R88.4)', () => {
  it('roundtrips with enc:v1: prefix', () => {
    const stored = encodeApiKey('sk-secret', fake)
    expect(stored.startsWith(ENC_PREFIX)).toBe(true)
    expect(decodeApiKey(stored, fake)).toBe('sk-secret')
  })
  it('legacy plaintext passes through decode unchanged', () => {
    expect(decodeApiKey('legacy-plain', fake)).toBe('legacy-plain')
  })
  it('empty key stays empty; codec unavailable → plaintext fallback', () => {
    expect(encodeApiKey('', fake)).toBe('')
    const offline: SafeStorageCodec = { encrypt: () => null, decrypt: () => null }
    expect(encodeApiKey('k', offline)).toBe('k')
  })
  it('corrupt ciphertext decodes to empty string, never throws', () => {
    expect(decodeApiKey(ENC_PREFIX + '!!!not-base64-safe===', fake)).toBe('')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `yarn test tests/main/aiCleanupService.test.ts tests/main/aiSecretCodec.test.ts`
Expected: 新用例 FAIL

- [ ] **Step 3: 实现**

`src/main/aiSecretCodec.ts`：

```ts
// R88.4: at-rest protection for the AI api key (Electron safeStorage / DPAPI).
// Pure codec functions with an injected implementation so they are unit-testable.

export const ENC_PREFIX = 'enc:v1:'

export interface SafeStorageCodec {
  encrypt(plain: string): Uint8Array | null
  decrypt(data: Uint8Array): string | null
}

export function encodeApiKey(plain: string, codec: SafeStorageCodec): string {
  if (plain === '') return ''
  const enc = codec.encrypt(plain)
  if (!enc) return plain // safeStorage unavailable → plaintext fallback (+warn at call site)
  return ENC_PREFIX + Buffer.from(enc).toString('base64')
}

export function decodeApiKey(stored: string, codec: SafeStorageCodec): string {
  if (!stored.startsWith(ENC_PREFIX)) return stored // legacy plaintext (migrated on next save)
  try {
    return codec.decrypt(new Uint8Array(Buffer.from(stored.slice(ENC_PREFIX.length), 'base64'))) ?? ''
  } catch {
    return ''
  }
}
```

`aiCleanupService.ts` 改造（保持既有导出不动，新增/替换如下）：
1. `DEFAULT_AI_SETTINGS.model` → `'glm-5.3-flash'`
2. 追加：

```ts
import type { AiChatMessage, AiChatOutcome } from '../shared/types'

/** R88: generic OpenAI-compatible chat pipeline (fetch + latency + hint taxonomy). */
export async function chatCompletion(
  messages: AiChatMessage[],
  s: AiCleanupSettings,
  opts?: { maxTokens?: number; temperature?: number; timeoutMs?: number },
): Promise<AiChatOutcome> {
  if (!s.apiKey.trim()) return { ok: false, text: '', hint: 'nokey', latencyMs: 0 }
  if (messages.length === 0) return { ok: false, text: '', hint: 'parse', latencyMs: 0 }
  const base = s.baseUrl.trim().replace(/\/+$/, '')
  const startedAt = Date.now()
  try {
    const res = await fetch(`${base}/chat/completions`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${s.apiKey.trim()}` },
      body: JSON.stringify({
        model: s.model.trim() || DEFAULT_AI_SETTINGS.model,
        messages,
        temperature: opts?.temperature ?? 0.1,
        ...(opts?.maxTokens !== undefined ? { max_tokens: opts.maxTokens } : {}),
      }),
      signal: AbortSignal.timeout(opts?.timeoutMs ?? 30_000),
    })
    const latencyMs = Date.now() - startedAt
    if (!res.ok) return { ok: false, text: '', hint: res.status === 401 || res.status === 403 ? 'auth' : 'http', latencyMs }
    const parsed = parseCleanupResponse(await res.json())
    if (parsed === null) return { ok: false, text: '', hint: 'parse', latencyMs }
    return { ok: true, text: parsed, latencyMs }
  } catch {
    return { ok: false, text: '', hint: 'network', latencyMs: Date.now() - startedAt }
  }
}

export function buildTestMessages(): AiChatMessage[] {
  return [{ role: 'user', content: 'ping' }]
}

export async function testConnection(s: AiCleanupSettings): Promise<AiChatOutcome> {
  return chatCompletion(buildTestMessages(), s, { maxTokens: 8 })
}
```

3. `cleanupOcrText`/`translateOcrText` 改为复用（行为等价，温度/超时同旧值），`postChat` 删除：

```ts
export async function cleanupOcrText(text: string, s: AiCleanupSettings): Promise<CleanupOutcome> {
  const out = await chatCompletion([{ role: 'system', content: CLEANUP_SYSTEM_PROMPT }, { role: 'user', content: text }], s)
  return { ok: out.ok, text: out.text, hint: out.hint }
}
export async function translateOcrText(text: string, s: AiCleanupSettings): Promise<CleanupOutcome> {
  const dir = detectTranslateDirection(text)
  const prompt = dir === 'zh2en'
    ? '你是翻译助手。把用户提供的中文文本翻译成英文。只输出译文，保留原文的段落与换行，不要任何解释。'
    : '你是翻译助手。把用户提供的英文文本翻译成中文。只输出译文，保留原文的段落与换行，不要任何解释。'
  const out = await chatCompletion([{ role: 'system', content: prompt }, { role: 'user', content: text }], s)
  return { ok: out.ok, text: out.text, hint: out.hint }
}
```

（`buildCleanupRequest`/`buildTranslateRequest` 保留导出——空文本早退语义由 chatCompletion 的 messages 非空保证：调用方 OCR 面板对空文本仍会发——保持旧 nokey/空文本行为一致，空文本时 translate 方向检测照跑但 chatCompletion 会真实发出；**注意**：旧实现空文本 → buildXxxRequest null → nokey。为完全等价，两个函数开头保留 `if (!text.trim()) return { ok: false, text: '', hint: 'nokey' }`。）

- [ ] **Step 4: 验证（新用例 + 既有用例都不破）+ 提交**

Run: `yarn test tests/main/ && yarn typecheck`
Expected: 全 PASS（含 R83/R84 既有用例）+ 0 error

```bash
git add src/main tests/main
git commit -m "[PRD-0002] feat: R88 chatCompletion 通用管线 + aiSecretCodec + 默认模型 glm-5.3-flash"
```

---

### Task 3: main/preload IPC 接线（+2 handler + safeStorage）

**Files:**
- Modify: `src/main/index.ts`、`src/preload/index.ts`、`tests/renderer/_helpers.tsx`

**Interfaces:**
- Consumes: Task 1/2 全部
- Produces: `window.rgbbox.aiTestConnection(): Promise<AiChatOutcome>`、`window.rgbbox.aiChat(messages: AiChatMessage[]): Promise<AiChatOutcome>`（校验失败 → `{ok:false,text:'',hint:'parse',latencyMs:0}`）

- [ ] **Step 1: main/index.ts**

顶部 import 区加 `import { safeStorage } from 'electron'`（并入现有 electron import）与 `import { encodeApiKey, decodeApiKey, type SafeStorageCodec } from './aiSecretCodec'`、`import { chatCompletion, testConnection } from './aiCleanupService'`（若 chatCompletion 未在 index 用则只引 testConnection）、`import { validateChatMessages } from '../shared/aiChatValidation'`。

在 aiTranslateText handler 后追加：

```ts
  // R88.2: AI Lab — connection test + multi-turn chat (validates payload, never throws)
  const safeStorageCodec: SafeStorageCodec = {
    encrypt: (plain) => {
      try {
        return safeStorage.isEncryptionAvailable() ? safeStorage.encryptString(plain) : null
      } catch {
        return null
      }
    },
    decrypt: (data) => {
      try {
        return safeStorage.decryptString(Buffer.from(data))
      } catch {
        return null
      }
    },
  }
  const AI_CHAT_INVALID = { ok: false, text: '', hint: 'parse' as const, latencyMs: 0 }

  ipcMain.handle(ipcChannels.aiTestConnection, async () => {
    const s = await loadSystemSettings()
    return testConnection(asAiSettings(s.ai))
  })
  ipcMain.handle(ipcChannels.aiChat, async (_event, p: unknown) => {
    const messages = validateChatMessages(p)
    if (messages === null) return AI_CHAT_INVALID
    const s = await loadSystemSettings()
    return chatCompletion(messages, asAiSettings(s.ai), { temperature: 0.7 })
  })
```

同时给现有两个 settings handler 接编解码（`asAiSettings` 定义改为读侧 decode；`aiSetSettings` 存侧 encode）：

```ts
  const asAiSettings = (ai: SystemSettings['ai']): AiCleanupSettings => ({
    baseUrl: typeof ai?.baseUrl === 'string' ? ai.baseUrl : DEFAULT_AI_SETTINGS.baseUrl,
    apiKey: decodeApiKey(typeof ai?.apiKey === 'string' ? ai.apiKey : '', safeStorageCodec),
    model: typeof ai?.model === 'string' ? ai.model : DEFAULT_AI_SETTINGS.model,
  })
  // aiSetSettings 内：
  const cfg: AiCleanupSettings = {
    baseUrl: …（不变）,
    apiKey: encodeApiKey(typeof q?.apiKey === 'string' ? q.apiKey.trim() : '', safeStorageCodec),
    model: …（不变）,
  }
  if (!safeStorage.isEncryptionAvailable()) console.warn('[RGBBox] safeStorage unavailable — AI key stored in plaintext')
```

- [ ] **Step 2: preload/index.ts**（aiTranslateText 之后追加；顶部 `import { validateChatMessages } from '../shared/aiChatValidation'`，类型 `import type { AiChatMessage, AiChatOutcome } from '../shared/types'`）

```ts
  aiTestConnection: (): Promise<AiChatOutcome> =>
    ipcRenderer.invoke(ipcChannels.aiTestConnection),
  aiChat: (messages: AiChatMessage[]): Promise<AiChatOutcome> => {
    const valid = validateChatMessages(messages)
    return valid === null
      ? Promise.resolve({ ok: false, text: '', hint: 'parse', latencyMs: 0 })
      : ipcRenderer.invoke(ipcChannels.aiChat, valid)
  },
```

- [ ] **Step 3: `tests/renderer/_helpers.tsx`** mock 增两行（aiTranslateText 行后）：

```ts
    aiTestConnection: vi.fn().mockResolvedValue({ ok: true, text: 'pong', latencyMs: 12 }),
    aiChat: vi.fn().mockResolvedValue({ ok: true, text: 'hi there', latencyMs: 20 }),
```

- [ ] **Step 4: 验证 + 提交**

Run: `yarn typecheck && yarn test tests/renderer/_helpers.tsx 2>/dev/null || yarn test tests/renderer/components/SettingsView.test.tsx`
Expected: 0 error；无回归（_helpers 无独立测试则跑任一组件测试确认 mock 不炸）

```bash
git add src/main/index.ts src/preload/index.ts tests/renderer/_helpers.tsx
git commit -m "[PRD-0002] feat: R88 IPC 接线——aiTestConnection/aiChat + safeStorage 编解码"
```

---

### Task 4: 渲染接入准备（i18n + 图标桩 + rail/磁贴注册）

**Files:**
- Modify: `src/renderer/src/i18n/index.tsx`、`tests/renderer/setup.ts`、`src/renderer/src/hooks/tabNavigation.ts`、`src/renderer/src/components/shellModules.ts`
- Test: 更新 `tests/renderer/i18nShellKeys.test.ts`、`tests/renderer/components/shellModules.test.ts`

**Interfaces:**
- Produces: `'ai'` 进入 View/MODULE_VIEWS/CARD_VIEWS/MODULE_META；i18n key 集

- [ ] **Step 1: 更新两个测试（先红）**

`i18nShellKeys.test.ts` SHELL_KEYS 增：`'nav.ai'`、`'dash.desc.ai'`、`'ai.privacyNote'`、`'ai.lab.provider'`、`'ai.lab.providerCustom'`、`'ai.lab.group.connection/config/chat/ocr'`、`'ai.lab.status.connected/disconnected/failed'`、`'ai.lab.test/showKey/hideKey'`、`'ai.lab.chat.placeholder/send/clear'`、`'ai.lab.ocr.input/result/cleanup/translate'`、`'ai.lab.save/reset'`；删 `'settings.group.ai'`。

`shellModules.test.ts` 的 order 用例同步（`[...CARD_VIEWS, 'settings']` 自动跟随，无需改断言——但「every card view has meta」会要求 ai 的 meta，实现前红）。

- [ ] **Step 2: 跑红确认**

Run: `yarn test tests/renderer/i18nShellKeys.test.ts tests/renderer/components/shellModules.test.ts`
Expected: FAIL

- [ ] **Step 3: 实现**

1. i18n EN/ZH 按上述 key 全量添加（zh：AI 实验室 / LLM 验证与会话试玩 / 隐私说明（spec §3.4 原文）/ 服务商 / 自定义 / 连接状态 / 模型配置 / 对话试玩 / OCR·翻译试玩 / 已连接 / 未测试 / 连接失败 / 测试连接 / 显示密钥 / 隐藏密钥 / 输入消息… / 发送 / 清空会话 / 输入或粘贴 OCR 文本 / 整理结果 / AI 整理 / 中英互译 / 保存 / 恢复默认；en 镜照）；删 `settings.group.ai`（两表）。
2. `setup.ts` NAMES 末尾追加 `'Bot'`。
3. `tabNavigation.ts`：`MODULE_VIEWS` 数组 `'architecture',` 后、`'settings'` 前插 `'ai',`。
4. `shellModules.ts`：import `Bot`；`CARD_VIEWS` 同位插 `'ai'`；`MODULE_META` 增 `ai: { view: 'ai', labelKey: 'nav.ai', descKey: 'dash.desc.ai', icon: Bot }`。

- [ ] **Step 4: 验证 + 提交**

Run: `yarn test tests/renderer/i18nShellKeys.test.ts tests/renderer/components/shellModules.test.ts tests/renderer/hooks/tabNavigation.test.ts && yarn typecheck`
Expected: 全 PASS + 0 error

```bash
git add src/renderer/src tests/renderer
git commit -m "[PRD-0002] feat: R88 渲染接入准备——i18n/Bot 桩/ai 模块注册"
```

---

### Task 5: AiLabView 组件（四区，自管状态）

**Files:**
- Create: `src/renderer/src/components/AiLabView.tsx`
- Test: `tests/renderer/components/AiLabView.test.tsx`

**Interfaces:**
- Consumes: `AI_PROVIDER_PRESETS/matchProviderPreset`、`AiChatMessage/AiChatOutcome`、`window.rgbbox.ai*` 全家
- Produces: `AiLabView(): JSX.Element`（无 props）

- [ ] **Step 1: 写失败测试（核心行为）**

```tsx
// tests/renderer/components/AiLabView.test.tsx
// @vitest-environment happy-dom
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { render, cleanup, fireEvent, waitFor } from '@testing-library/react'
import { AiLabView } from '../../../src/renderer/src/components/AiLabView'
import { setupRendererMocks } from '../_helpers'

beforeEach(() => { localStorage.clear(); cleanup() })

function mount() {
  const rgbbox = setupRendererMocks()
  rgbbox.aiGetSettings.mockResolvedValue({ baseUrl: 'https://open.bigmodel.cn/api/paas/v4', apiKey: 'sk-x', model: 'glm-5.3' })
  return { rgbbox, ...render(<AiLabView />) }
}

describe('AiLabView (R88)', () => {
  it('renders four collapsible groups', async () => {
    const { container, findByText } = mount()
    const summaries = container.querySelectorAll('.dash-group > summary')
    expect(summaries.length).toBe(4)
    expect([...summaries].map((s) => s.textContent)).toEqual([
      expect.stringContaining('ai.lab.group.connection'),
      expect.stringContaining('ai.lab.group.config'),
      expect.stringContaining('ai.lab.group.chat'),
      expect.stringContaining('ai.lab.group.ocr'),
    ])
  })

  it('loads settings and reverse-matches the provider preset', async () => {
    const { container, findByDisplayValue } = mount()
    expect(await findByDisplayValue('https://open.bigmodel.cn/api/paas/v4')).toBeTruthy()
    const providerSelect = container.querySelector('select[data-field="provider"]') as HTMLSelectElement
    expect(providerSelect.value).toBe('zhipu')
    const modelInput = container.querySelector('input[data-field="model"]') as HTMLInputElement
    expect(modelInput.value).toBe('glm-5.3')
  })

  it('key input is masked with a toggle and shows the privacy note', async () => {
    const { container, findByDisplayValue } = mount()
    const keyInput = (await findByDisplayValue('sk-x')) as HTMLInputElement
    expect(keyInput.type).toBe('password')
    expect(keyInput.autocomplete).toBe('new-password')
    expect(container.textContent).toContain('ai.privacyNote')
    fireEvent.click(container.querySelector('[data-action="toggle-key"]') as HTMLElement)
    expect(keyInput.type).toBe('text')
  })

  it('picking a provider preset fills baseUrl + first model', async () => {
    const { container, findByDisplayValue } = mount()
    await findByDisplayValue('https://open.bigmodel.cn/api/paas/v4')
    fireEvent.change(container.querySelector('select[data-field="provider"]') as HTMLSelectElement, { target: { value: 'deepseek' } })
    expect((container.querySelector('input[data-field="baseUrl"]') as HTMLInputElement).value).toBe('https://api.deepseek.com')
    expect((container.querySelector('input[data-field="model"]') as HTMLInputElement).value).toBe('deepseek-v4-pro')
  })

  it('save persists and auto-runs the connection test', async () => {
    const { rgbbox, container, findByDisplayValue } = mount()
    rgbbox.aiSetSettings.mockResolvedValue({ baseUrl: 'b', apiKey: 'k', model: 'm' })
    await findByDisplayValue('https://open.bigmodel.cn/api/paas/v4')
    fireEvent.click(container.querySelector('[data-action="save"]') as HTMLElement)
    await waitFor(() => expect(rgbbox.aiSetSettings).toHaveBeenCalled())
    await waitFor(() => expect(rgbbox.aiTestConnection).toHaveBeenCalled())
  })

  it('chat: sends a turn, appends user+assistant with latency, clear wipes', async () => {
    const { rgbbox, container, findByDisplayValue } = mount()
    await findByDisplayValue('https://open.bigmodel.cn/api/paas/v4')
    const input = container.querySelector('textarea[data-field="chat-input"]') as HTMLTextAreaElement
    fireEvent.change(input, { target: { value: '你好' } })
    fireEvent.click(container.querySelector('[data-action="send"]') as HTMLElement)
    await waitFor(() => expect(rgbbox.aiChat).toHaveBeenCalled())
    expect(rgbbox.aiChat.mock.calls[0][0].at(-1)).toEqual({ role: 'user', content: '你好' })
    await waitFor(() => expect(container.querySelectorAll('.ai-msg').length).toBe(2))
    expect(container.querySelector('.ai-status')?.textContent).toContain('已连接') // zh fallback? — 改断言为 latency 数字
    fireEvent.click(container.querySelector('[data-action="clear-chat"]') as HTMLElement)
    expect(container.querySelectorAll('.ai-msg').length).toBe(0)
  })

  it('ocr playground: cleanup + translate call the existing IPCs', async () => {
    const { rgbbox, container, findByDisplayValue } = mount()
    rgbbox.aiCleanupText.mockResolvedValue({ ok: true, text: 'cleaned' })
    await findByDisplayValue('https://open.bigmodel.cn/api/paas/v4')
    const input = container.querySelector('textarea[data-field="ocr-input"]') as HTMLTextAreaElement
    fireEvent.change(input, { target: { value: 'raw text' } })
    fireEvent.click(container.querySelector('[data-action="ocr-cleanup"]') as HTMLElement)
    await waitFor(() => expect(rgbbox.aiCleanupText).toHaveBeenCalledWith('raw text'))
    await waitFor(() => expect(container.querySelector('.ai-ocr-result')?.textContent).toContain('cleaned'))
  })
})
```

（注意：chat 断言里的 `已连接` 一行是笔误占位——**实际实现时改为断言** `container.querySelector('.ai-status')?.textContent` **匹配 `/12|20|\d+ ?ms/`**，mock latencyMs=20。）

- [ ] **Step 2: 跑红**

Run: `yarn test tests/renderer/components/AiLabView.test.tsx`
Expected: FAIL（组件不存在）

- [ ] **Step 3: 实现 AiLabView（要点）**

结构：外层 `.settings-view` 风格容器（`.ai-lab`）+ 四个 `<details open className="dash-group">`（复用 R86 折叠组样式与箭头）。状态：`cfg/showKey/conn{state,ok,latexMs,hint}/testing/chat[]/chatInput/chatBusy/ocrInput/ocrResult/ocrBusy`。mount `aiGetSettings` 载入。行为要点：
- provider select：值 = `matchProviderPreset(cfg.baseUrl).id`；onChange → 非 custom 填 `baseUrl`+`models[0]`，custom 只清 baseUrl；选项 label = `preset.label || t('ai.lab.providerCustom')`
- 模型：`input[data-field=model]` + `<datalist>`（当前预设 models）
- key：password/new-password/spellcheck=false + `[data-action=toggle-key]` + `ai.privacyNote` 行（`ai-privacy-note` 类）
- `[data-action=save]`：`aiSetSettings(cfg)` → 成功后自动 `aiTestConnection()`；`[data-action=reset]`：恢复 zhipu 默认（baseUrl+glm-5.3-flash，key 清空）
- 连接状态区：`ai-status` 行（idle/ok(+latencyMs+model)/fail(hint 文案)）+ `[ai.lab.test]` 按钮（testing 时 disabled）
- chat：`ai-msg` 气泡（role 标识 + assistant 带 `(latencyMs ms)`）；发送 = `aiChat([...chat, {role:'user',content}])` → ok 时 push user+assistant；失败 push user + 错误行（hint 文案）；busy 防重入；`clear-chat` 清空
- ocr：`ai-ocr-result` 结果区；两个按钮分别调 `aiCleanupText/aiTranslateText`；hint 失败文案复用 `ai.hint`（不存在则显示 hint 原文——**实现时先 grep i18n 现有 `ai.` hint key 名并以实际为准**）
- 失败/未配 key：各区块顶部复用同一 `AiHintLine`（本地小组件，渲染 hint 文案）

- [ ] **Step 4: 验证 + 提交**

Run: `yarn test tests/renderer/components/AiLabView.test.tsx && yarn typecheck`
Expected: 全 PASS + 0 error

```bash
git add src/renderer/src/components/AiLabView.tsx tests/renderer/components/AiLabView.test.tsx
git commit -m "[PRD-0002] feat: R88 AiLabView 四区（连接/配置/会话/OCR 试玩）"
```

---

### Task 6: App/SettingsView 接线与瘦身（原子）

**Files:**
- Modify: `src/renderer/src/App.tsx`、`src/renderer/src/components/SettingsView.tsx`
- Test: 更新 `tests/renderer/components/SettingsView.test.tsx`

**Interfaces:**
- Consumes: Task 4/5
- Produces: `{activeView === 'ai' && <AiLabView />}`；SettingsView 四组→三组（props 删 ai 五项）

- [ ] **Step 1: 更新 SettingsView 测试（先红）**：四组断言改三组（run/screensaver/hotkey），删 ai 组用例。

- [ ] **Step 2: 实现**
1. `SettingsView.tsx`：删 `AiCfg` interface 与 props 中 ai 五项、AI `<section data-group="ai">` 整块。
2. `App.tsx`：
   - import `AiLabView`；
   - `grep -n "aiCfg\|aiSaved\|saveAiCfg" src/renderer/src/App.tsx` 定位全部引用：删 state 定义（~738-743）、初始化 effect 中的 aiGetSettings 项、SettingsView JSX 的 ai props；
   - settings 渲染块后加 `{activeView === 'ai' && <AiLabView />}`。
3. i18n 的 `ai.baseUrl/ai.model/ai.apiKey/ai.save/ai.saved/ai.label/ai.hint` 若仅 SettingsView 使用则保留（AiLabView 复用 `ai.save`；`settings.group.ai` 已在 T4 删）。

- [ ] **Step 3: 验证 + 提交**

Run: `yarn typecheck && yarn test`
Expected: 0 error + 全量 0 失败

```bash
git add src/renderer/src tests/renderer
git commit -m "[PRD-0002] feat: R88 App 接线 + 设置页 AI 组迁走（原子）"
```

---

### Task 7: 清理核查 + 全量回归

- [ ] **Step 1: 死引用核查**

```bash
grep -rn "settings.group.ai\|aiCfg\|saveAiCfg\|aiSaved" src/renderer/src tests | grep -v node_modules
```

Expected: 零命中（AiLabView 内部状态命名避开 aiCfg）。

- [ ] **Step 2: 全量回归**

Run: `yarn typecheck && yarn test && yarn build`
Expected: 0 error；0 失败；build 成功。

- [ ] **Step 3: 手动冒烟（可选）**：`yarn dev` → rail AI 入口 → 预设选智谱（glm-5.3/glm-5.3-flash 可见）→ 填 key 保存（自动测连接）→ 对话两轮 → OCR 试玩 → 重启后 key 反显、落盘 `%APPDATA%/rgbbox/system.json` 中 apiKey 为 `enc:v1:` 前缀。

---

### Task 8: PRD R88 收尾

- [ ] **Step 1:** R88.9 状态改 ✅ + 证据（测试数/typecheck/build/死引用核查；实机待用户）。
- [ ] **Step 2:** 提交 `[PRD-0002] docs: R88 验收完成，状态 ✅ + 证据`。

---

## Self-Review 记录（已核对）

1. **Spec 覆盖**：§3.1→T4/T6；§3.2→T1/T3；§3.3→T2；§3.4→T2/T3/T5；§3.5→T1/T5；§4→T5/T6；§5→T5；§6→T4；§7→各任务；§8→T7 Step 3 + T8。无缺口。
2. **占位符**：T5 Step 1 内已自我纠正一处笔误占位（chat 断言改为 latency 数字断言，并注明）；ai.hint key 名标注「以实际 grep 为准」（现有 key 存在性待实现时核对——R84 复用行存在）。
3. **类型一致**：`AiChatOutcome/AiChatMessage`（T1 定义，T2/T3/T5 消费）；`SafeStorageCodec`（T2 定义，T3 消费）；`validateChatMessages`（T1 定义，T3 消费）；`MODULE_VIEWS/CARD_VIEWS` 同位插 `'ai'`（T4，order 测试锁同步）。
